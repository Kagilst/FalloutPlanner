<Application x:Class="FalloutPlanner.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FalloutPlanner"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Fallout Color Pallette -->
        <!-- Core colors -->
        <SolidColorBrush x:Key="FalloutBackgroundBrush" Color="#08130A" />
        <SolidColorBrush x:Key="FalloutPanelBrush" Color="#0E1F12" />
        <SolidColorBrush x:Key="FalloutAccentBrush" Color="#88FF00" />
        <SolidColorBrush x:Key="FalloutAccentDarkBrush" Color="#447700" />
        <SolidColorBrush x:Key="FalloutBorderBrush" Color="#335500" />
        <SolidColorBrush x:Key="FalloutTextBrush" Color="#CFFF9A" />

        <!-- Default window styling -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource FalloutBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource FalloutTextBrush}" />
            <Setter Property="FontFamily" Value="Consolas" />
        </Style>

        <!-- Button styling -->
        <Style x:Key="BoxButtons" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource FalloutPanelBrush}" />
            <Setter Property="Foreground" Value="{StaticResource FalloutTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource FalloutBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="3" CornerRadius="1"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                Property="Background"
                                Value="{StaticResource FalloutAccentBrush}" />
                                <Setter Property="Foreground"
                                Value="Black"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border"
                                Property="Background"
                                Value="{StaticResource FalloutAccentDarkBrush}" />
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Text styling -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FalloutTextBrush}" />
        </Style>

        <!-- Increase or Decrease button styling -->
        <Style TargetType="Button" x:Key="DecreaseIncreaseButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource FalloutTextBrush}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" TextElement.Foreground="{TemplateBinding Foreground}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#004d00"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="#004d00"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Toggle button styling -->
        <Style TargetType="{x:Type ToggleButton}" x:Key="TAG">
            <Setter Property="Background" Value="#1a282828"/>
            <Setter Property="BorderBrush" Value="{StaticResource FalloutBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        Background="{TemplateBinding Background}"
                        CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="{StaticResource FalloutBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{StaticResource FalloutAccentBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource FalloutBorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
